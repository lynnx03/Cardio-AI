# ── Secrets / environment ──
.env
.env.*
!.env.example

# ── Python ──
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# ── Virtual environments ──
venv/
.venv/
env/
ENV/

# ── Data ──
# Kaggle Heart Failure dataset — download separately to retrain (see README).
heart.csv

# NOTE: model.pkl and model_quiz.pkl ARE committed on purpose, so the app
# runs on a fresh clone without retraining. They are not ignored.

# ── Editors / IDEs ──
.vscode/
.idea/
*.swp
*.swo

# ── OS ──
.DS_Store
Thumbs.db
